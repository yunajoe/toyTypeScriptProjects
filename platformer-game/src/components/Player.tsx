function Player() {
  // player에게 필요한건?
  // player의 크기 (width, height)
  // playerr의 현재 위치
  // player의 움직인 거리
  class Player {
    width: number;
    height: number;

    constructor(width: number, height: number) {
      this.width = width;
      this.height = height;
    }
  }
  return <div></div>;
}

export default Player;
