import { globalVars } from "@/theme.css";
import { style } from "@vanilla-extract/css";

export const buttonStyles = style({
  padding: "5px",
  fontSize: globalVars.fontSize.small,
  fontWeight: globalVars.fontWeight.medium,
});
